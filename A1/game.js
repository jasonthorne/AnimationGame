
//import * as background from "./background.js";
//import {basketCanvas, basketImg, basketCtx} from "./basket.js";



//after background image load:
/*backgroundImg.onload = () =>{ //draw background:
    backgroundCtx.drawImage(backgroundImg, 0, 0, backgroundCanvas.width, backgroundCanvas.height);
    basketCtx.drawImage( //then draw basket:
        basketImg, (basketCanvas.width - basketImg.width) /2, 
        (basketCanvas.height - basketImg.height) - 10, 
        basketImg.width, basketImg.height);
}*/




const maxApples = 5; //+++++??????





export class Game {

	constructor(){
                //set canvas element:
                ////document.getElementById("layers-container").replaceChildren(canvas);
                //buildCanvases(maxApples);
	}
	
}

//canvas



